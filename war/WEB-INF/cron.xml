<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
  <cron>
    <url>/keepalive</url>
    <description>Keep the application alive so the cache is active</description>
    <schedule>every 2 minutes</schedule>
  </cron>
</cronentries>